pipeline {
agent any
environment {
IMAGE_NAME = 'flaskapp:v1'
}
stages {
stage('Clone Code') {
steps {
git 'https://github.com/shameerkhan734-spec/projecthealthcare.git'
}
}
stage('Build Docker Image') {
steps {
sh 'docker build -t $IMAGE_NAME .'
}
}
stage('Run Docker Container') {
steps {
sh 'docker rm -f flaskapp || true'
sh 'docker run -d --name flaskapp -p 5000:5000 $IMAGE_NAME'
}
}
}
post {
success {
echo '✅ App deployed successfully at http://<3.85.243.164>:5000'
}
failure {
echo '❌ Build failed.'
}
}
}
